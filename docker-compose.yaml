version: "3.9"
   
services:
  app:
    build: .
    command: flask --bind 0.0.0.0:5000 app:app
    ports:
      - "5000:5000"
    volumes:
      - .:/code
    restart: always
  nginx:
    image: nginx:1.19.3
    ports:
      - "80:80"
    volumes:
      - ./nginx.conf:/etc/nginx/conf.d/default.conf
    depends_on:
      - app
    restart: always
